% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-6.R
\name{plot_num_auc}
\alias{plot_num_auc}
\title{Plot AUC vs. Number of Features for Multiple Methods}
\usage{
plot_num_auc(
  combined_plot_data,
  best_features_num,
  palette_name = "AsteroidCity1",
  base_size = 14,
  save_plots = TRUE,
  save_dir = here::here("ModelData", "sel_feature"),
  plot_width = 5,
  plot_height = 5
)
}
\arguments{
\item{combined_plot_data}{A data frame containing columns \code{Num_Features}, \code{AUC}, and \code{Method},
representing the number of features, AUC values, and corresponding method names.}

\item{best_features_num}{An integer indicating the optimal number of features to highlight in the plot.}

\item{palette_name}{Character string specifying the palette name used by \code{wes_palette()} for color mapping.}

\item{base_size}{Numeric value indicating the base font size for the plot theme.}

\item{save_plots}{Logical value indicating whether to save the generated plot as a PDF. Default is \code{TRUE}.}

\item{save_dir}{Path to the directory where the plot should be saved. Defaults to \code{here("ModelData", "sel_feature")}.}

\item{plot_width}{Numeric value specifying the width (in inches) of the output plot. Default is 5.}

\item{plot_height}{Numeric value specifying the height (in inches) of the output plot. Default is 5.}
}
\value{
A ggplot object showing AUC curves for each method across different numbers of features.
}
\description{
This function visualizes the relationship between the number of selected features and the corresponding
AUC (Area Under the Curve) values across different feature selection methods. It also highlights the optimal
number of features using a dashed vertical line. Optionally, the plot can be saved as a PDF.
}
\examples{
\dontrun{
library(ggplot2)
library(wesanderson)
library(ggprism)
example_data <- data.frame(
  Num_Features = rep(1:10, 3),
  AUC = runif(30, 0.6, 0.9),
  Method = rep(c("IV", "MI", "MIC"), each = 10)
)
plot_num_auc(example_data, best_features_num = 5)
}
}
