% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-13.R
\name{ModelClinicalPrediction}
\alias{ModelClinicalPrediction}
\title{Clinical Prediction Using a Trained Model_data Object}
\usage{
ModelClinicalPrediction(
  object,
  new_data,
  group_col = "group",
  palette_name = "Royal1",
  save_dir = here("ModelData", "clinical_predictions"),
  plot_width = 6,
  plot_height = 6,
  alpha = 1,
  base_size = 14,
  best_threshold = NULL
)
}
\arguments{
\item{object}{An object of class \code{Model_data} containing the best model and training information.}

\item{new_data}{A \code{data.frame} or tibble containing new clinical samples to be predicted. Column names should match model features.}

\item{group_col}{Character. Column name used for grouping in the output plot (optional, not directly used in this function). Default is \code{"group"}.}

\item{palette_name}{Character. Name of the color palette used for the visualization. Default is \code{"Royal1"} from the \code{wesanderson} package.}

\item{save_dir}{Character. Path to the directory where the prediction plot will be saved. Default is \code{here("ModelData", "clinical_predictions")}.}

\item{plot_width}{Numeric. Width of the saved plot in inches. Default is 6.}

\item{plot_height}{Numeric. Height of the saved plot in inches. Default is 6.}

\item{alpha}{Numeric. Transparency level for the jitter points in the plot. Default is 1.}

\item{base_size}{Numeric. Base font size used in the plot. Default is 14.}

\item{best_threshold}{Numeric. Optional. Threshold for classifying predicted probabilities. If NULL, it will be extracted from the model. Default is \code{NULL}.}
}
\value{
A list containing:
\item{predictions}{A data frame with sample names, predicted class labels, and predicted probabilities.}
\item{best_threshold}{The threshold used for classification.}
}
\description{
Applies a trained model from a \code{Model_data} object to a new clinical dataset, performs classification based on predicted probabilities,
and visualizes the results using boxplots and jitter plots. Automatically handles missing features and factor conversion based on training data.
}
\examples{
\dontrun{
data("example_model_data")
new_clinical_data <- read.csv("new_patient_data.csv")
prediction_result <- ModelClinicalPrediction(
  object = example_model_data,
  new_data = new_clinical_data
)
head(prediction_result$predictions)
}
}
