% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-2.R
\name{handle_outliers_capping}
\alias{handle_outliers_capping}
\title{Cap Outliers to a Defined Range}
\usage{
handle_outliers_capping(data, lower_quantile = 0.05, upper_quantile = 0.95)
}
\arguments{
\item{data}{A data frame containing the original data with outlier columns
(columns with names ending in "_outlier").}

\item{lower_quantile}{Numeric value representing the lower quantile for capping
(default is 0.05).}

\item{upper_quantile}{Numeric value representing the upper quantile for capping
(default is 0.95).}
}
\value{
A data frame with outlier values capped to the specified quantile bounds.
}
\description{
This function caps outliers in the data by replacing values outside of the
specified quantile range with the nearest quantile values. It uses the
\code{lower_quantile} and \code{upper_quantile} arguments to define the bounds.
}
\examples{
# Example usage
data <- data.frame(gene1 = c(1, 2, 3, 100, 5),
                   gene1_outlier = c(FALSE, FALSE, FALSE, TRUE, FALSE),
                   gene2 = c(2, 4, 6, 100, 8),
                   gene2_outlier = c(FALSE, FALSE, FALSE, TRUE, FALSE))
result_data <- handle_outliers_capping(data, lower_quantile = 0.1, upper_quantile = 0.9)
# result_data will have outliers capped based on the specified quantiles.
}
