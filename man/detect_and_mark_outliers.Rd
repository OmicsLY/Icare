% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-4-outlier.R
\name{detect_and_mark_outliers}
\alias{detect_and_mark_outliers}
\title{Detect and Visualize Outliers in Numeric Variables}
\usage{
detect_and_mark_outliers(
  data,
  group_col = "group",
  palette_name = "Royal1",
  save_plots = FALSE,
  save_dir = here("StatObject"),
  plot_display_num = 1,
  sub_var = NULL,
  plot_width = 5,
  plot_height = 5,
  base_size = 14,
  custom_ranges = NULL,
  max_unique_values = 5
)
}
\arguments{
\item{data}{Data frame containing the variables to analyze}

\item{group_col}{Name of grouping variable (factor) for colored boxplots (default: "group")}

\item{palette_name}{Name of color palette for visualization (default: "Royal1")}

\item{save_plots}{Logical indicating whether to save plots (default: FALSE)}

\item{save_dir}{Directory path to save plots (default: here("StatObject"))}

\item{plot_display_num}{Number of variables to display per plot (default: 1)}

\item{sub_var}{Optional subset of variables to analyze (default: NULL)}

\item{plot_width}{Plot width in inches (default: 5)}

\item{plot_height}{Plot height in inches (default: 5)}

\item{base_size}{Base font size for plots (default: 14)}

\item{custom_ranges}{Named list of custom ranges for variables (default: NULL)}

\item{max_unique_values}{Maximum unique values for numeric variable detection (default: 5)}
}
\value{
A list containing:
\itemize{
\item data_marked - Original data frame with added outlier indicators (_outlier columns)
\item plots - List of ggplot boxplot objects
\item normal_ranges - Data frame with IQR and boundary information
}
}
\description{
This function identifies outliers in numeric variables using the IQR method (with optional custom ranges),
marks them in the dataset, and generates boxplot visualizations. Supports grouping by a categorical variable.
}
\examples{
\dontrun{
# Basic usage
data(mtcars)
results <- detect_and_mark_outliers(mtcars, group_col = "cyl")

# With custom ranges
custom_ranges <- list(mpg = c(10, 30), hp = c(50, 200))
results <- detect_and_mark_outliers(mtcars, custom_ranges = custom_ranges)
}
}
