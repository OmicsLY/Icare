% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-18-Validation-performannce.R
\name{plot_validation_model_roc}
\alias{plot_validation_model_roc}
\title{Plot ROC Curve for External Validation Data}
\usage{
plot_validation_model_roc(
  best_model,
  validation_data,
  group_col = "group",
  palette_name = "AsteroidCity1",
  base_size = 14,
  save_plots = TRUE,
  save_dir = here("ModelData", "best_model_result"),
  plot_width = 5,
  plot_height = 5,
  alpha = 1,
  subtitle = "Validation Dataset"
)
}
\arguments{
\item{best_model}{A trained classification model object that supports probability prediction via \code{predict(..., type = "prob")}.}

\item{validation_data}{A data frame containing the validation dataset. Must include the true class labels in \code{group_col}.}

\item{group_col}{A string specifying the name of the column in \code{validation_data} that contains the true class labels. Default is "group".}

\item{palette_name}{A string specifying the color palette name used for plotting. Must be a valid palette in \code{wesanderson::wes_palette()}. Default is "AsteroidCity1".}

\item{base_size}{A numeric value controlling the base font size for the plot. Default is 14.}

\item{save_plots}{Logical; whether to save the plot as a PDF file. Default is TRUE.}

\item{save_dir}{A character string specifying the directory path where the plot should be saved. Default is \code{here("ModelData", "best_model_result")}.}

\item{plot_width}{Numeric value specifying the width (in inches) of the saved plot. Default is 5.}

\item{plot_height}{Numeric value specifying the height (in inches) of the saved plot. Default is 5.}

\item{alpha}{Numeric value (between 0 and 1) indicating the transparency level of the ROC curve line. Default is 1 (fully opaque).}

\item{subtitle}{A string to be displayed as the subtitle of the ROC plot. Default is "Validation Dataset".}
}
\value{
A data frame containing the coordinates of the ROC curve for plotting purposes, including specificity, sensitivity, and AUC information.
}
\description{
This function generates and optionally saves a ROC curve for an external validation dataset
using the best-performing classification model. It calculates the AUC and its confidence interval,
and produces a publication-ready ROC plot.
}
\examples{
# Assuming `model` is a trained classifier and `val_data` is a data frame for validation:
plot_validation_model_roc(
  best_model = model,
  validation_data = val_data,
  group_col = "outcome"
)
}
