% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-9.R
\name{ModelValidation}
\alias{ModelValidation}
\title{Validate Best Model on External Dataset and Generate ROC Curve}
\usage{
ModelValidation(
  object,
  group_col = "group",
  palette_name = "AsteroidCity1",
  base_size = 14,
  save_plots = TRUE,
  save_dir = here("ModelData", "best_model_result"),
  plot_width = 5,
  plot_height = 5,
  alpha = 0.05
)
}
\arguments{
\item{object}{An object of class \code{Model_data}, containing slots for the filtered training and validation datasets, as well as the best trained model (either as an object or file path).}

\item{group_col}{A character string specifying the name of the column indicating class labels in the validation dataset. Default is \code{"group"}.}

\item{palette_name}{A character string specifying the color palette used in ROC plotting. Default is \code{"AsteroidCity1"} from the \code{wesanderson} package.}

\item{base_size}{Numeric value controlling the base font size for the ROC plot. Default is \code{14}.}

\item{save_plots}{Logical. If \code{TRUE}, saves the generated ROC curve as a PDF file in \code{save_dir}. Default is \code{TRUE}.}

\item{save_dir}{A character string specifying the directory to which the plot should be saved. Default is \code{here("ModelData", "best_model_result")}.}

\item{plot_width}{Numeric value specifying the width (in inches) of the saved plot. Default is \code{5}.}

\item{plot_height}{Numeric value specifying the height (in inches) of the saved plot. Default is \code{5}.}

\item{alpha}{Numeric value (between 0 and 1) specifying the transparency level of the ROC curve. Default is \code{0.05}.}
}
\value{
Returns an updated \code{Model_data} object with validation results added to the \code{best.model.result} slot. Specifically, it includes:
\itemize{
\item \code{roc_plot}: A data frame with ROC curve data.
\item \code{validation_result}: The output from the \code{evaluate_model_performance()} function, typically including metrics like accuracy, sensitivity, specificity, AUC, etc.
}
}
\description{
This function takes a \code{Model_data} object, extracts the best model and validation dataset,
preprocesses the data (including imputation of missing predictor variables), evaluates the model performance,
generates the ROC curve on the validation data, and updates the original object with the validation results.
}
\examples{
\dontrun{
# Assuming `model_obj` is a Model_data object with model and validation data:
updated_obj <- ModelValidation(model_obj)
}
}
