% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-8-balance.R
\name{balance_data_process}
\alias{balance_data_process}
\title{Balance Dataset Using Various Sampling Methods}
\usage{
balance_data_process(
  data,
  group_col = "group",
  method = "both",
  N = NULL,
  K = 5,
  dup_size = 0,
  seed = 1234
)
}
\arguments{
\item{data}{A data frame containing the dataset to be balanced}

\item{group_col}{Character string specifying the name of the grouping variable (default: "group")}

\item{method}{Balancing method to use: "over" (oversampling), "under" (undersampling),
"both" (combined sampling), or "smote" (SMOTE algorithm) (default: "both")}

\item{N}{Desired sample size after balancing (NULL for automatic calculation)}

\item{K}{Number of nearest neighbors to consider for SMOTE (default: 5)}

\item{dup_size}{Oversampling rate for SMOTE (0 = balanced, 1 = double minority class, etc.)}

\item{seed}{Random seed for reproducibility (default: 1234)}
}
\value{
A data frame with balanced class distribution
}
\description{
This function balances imbalanced datasets using oversampling, undersampling,
combined sampling, or SMOTE (Synthetic Minority Oversampling Technique).
}
\examples{
\dontrun{
# Example usage:
data <- data.frame(
  group = factor(c(rep(0, 100), rep(1, 30))),
  var1 = rnorm(130),
  var2 = rnorm(130)
)

# Oversample minority class
balanced_over <- balance_data_process(data, method = "over")

# Undersample majority class
balanced_under <- balance_data_process(data, method = "under")

# Use SMOTE
balanced_smote <- balance_data_process(data, method = "smote")
}


}
