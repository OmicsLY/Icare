% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-5-normalize.R
\name{stat_normalize_process}
\alias{stat_normalize_process}
\title{Perform Normalization on Statistical Object or Data Frame}
\usage{
stat_normalize_process(
  object,
  normalize_method = "auto",
  group_col = "group",
  max_unique_values = 5,
  alpha = 0.05,
  save_dir = here::here("StatObject", "Data"),
  save_data = TRUE,
  csv_filename = "normalize_data.csv"
)
}
\arguments{
\item{object}{An object of class \code{Stat} (with slots \code{clean.data}, \code{group_col}, \code{scale.data},
and \code{normalization.info}) or a \code{data.frame}.}

\item{normalize_method}{The normalization method to apply. One of: \code{"auto"},
\code{"log_transform"}, \code{"min_max_scale"}, \code{"z_score_standardize"}, \code{"max_abs_scale"},
\code{"center_data"}, \code{"scale_data"}, \code{"boxcox_transform"}, \code{"yeojohnson_transform"}.}

\item{group_col}{The column name for grouping (optional, used for diagnostics).}

\item{max_unique_values}{Maximum unique values for categorical variable detection. Default is 5.}

\item{alpha}{Significance level for normality test in \code{"auto"} mode. Default is 0.05.}
}
\value{
If \code{object} is a \code{Stat} object, returns the updated object with \code{scale.data} and \code{normalization.info} slots updated.
If \code{object} is a \code{data.frame}, returns a list containing the normalized data and metadata.
}
\description{
This function normalizes the numeric variables in the provided object (either a \code{Stat} object
or a \code{data.frame}) using the specified normalization method. It automatically detects the
appropriate normalization strategy if \code{normalize_method = "auto"}.
}
\examples{
\dontrun{
# For data.frame input
result <- stat_normalize_process(my_data, normalize_method = "z_score_standardize")

# For Stat object
my_stat <- stat_normalize_process(my_stat_object)
}
}
