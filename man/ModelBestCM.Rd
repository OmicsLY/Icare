% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-10.R
\name{ModelBestCM}
\alias{ModelBestCM}
\title{Generate and Save Confusion Matrix for the Best Model in a \code{Model_data} Object}
\usage{
ModelBestCM(
  object,
  group_col = "group",
  palette_name = "AsteroidCity1",
  save_plots = TRUE,
  save_dir = here("ModelData", "best_model_result"),
  plot_width = 6,
  plot_height = 6
)
}
\arguments{
\item{object}{An object of class \code{Model_data}, which should contain a trained model and test dataset.}

\item{group_col}{A character string specifying the name of the column that contains the true class labels.
This argument is automatically overwritten by the value stored in the \code{object@group_col} slot. Default is \code{"group"}.}

\item{palette_name}{A character string specifying the name of the color palette used for plotting (from \code{wesanderson} package). Default is \code{"AsteroidCity1"}.}

\item{save_plots}{Logical. If \code{TRUE}, saves the confusion matrix plot as a PDF file. Default is \code{TRUE}.}

\item{save_dir}{A character string specifying the directory to save the plot. Default is \code{here("ModelData", "best_model_result")}.}

\item{plot_width}{Numeric. Width of the saved plot in inches. Default is \code{6}.}

\item{plot_height}{Numeric. Height of the saved plot in inches. Default is \code{6}.}
}
\value{
An updated object of class \code{Model_data} with the confusion matrix result stored in the \code{best.model.result} slot under the key \code{"confusion_matrix_results"}.
}
\description{
This function extracts the best classification model from a \code{Model_data} object,
generates the confusion matrix using the test dataset, and updates the object with the result.
Optionally, the confusion matrix plot can be saved as a PDF file.
}
\examples{
\dontrun{
# Assuming `model_obj` is an object of class `Model_data`:
model_obj <- ModelBestCM(model_obj)
print(model_obj@best.model.result[["confusion_matrix_results"]])
}
}
