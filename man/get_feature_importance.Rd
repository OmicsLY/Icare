% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-11.R
\name{get_feature_importance}
\alias{get_feature_importance}
\title{Extract and Plot Feature Importance from the Best Model}
\usage{
get_feature_importance(
  best_model,
  top_n = 15,
  palette_name = "AsteroidCity1",
  save_plots = TRUE,
  save_dir = here("ModelData", "best_model_result"),
  plot_width = 5,
  plot_height = 5,
  base_size = 14,
  seed = 123
)
}
\arguments{
\item{best_model}{A trained model object supported by the \code{varImp} function (e.g., models trained via the \code{caret} package).}

\item{top_n}{Integer. Number of top features to display. If \code{NULL} or greater than the number of features, all features will be displayed. Default is \code{15}.}

\item{palette_name}{Character. Name of the color palette to use for the bar chart (from the \code{wesanderson} package). Default is \code{"AsteroidCity1"}.}

\item{save_plots}{Logical. Whether to save the plot to a PDF file. Default is \code{TRUE}.}

\item{save_dir}{Character. Path to the directory where the plot will be saved. Default is \code{here("ModelData", "best_model_result")}.}

\item{plot_width}{Numeric. Width of the saved plot in inches. Default is \code{5}.}

\item{plot_height}{Numeric. Height of the saved plot in inches. Default is \code{5}.}

\item{base_size}{Numeric. Base font size for the plot theme. Default is \code{14}.}

\item{seed}{Integer. Random seed for reproducibility. Default is \code{123}.}
}
\value{
A \code{ggplot} object representing the feature importance plot.
}
\description{
This function extracts feature importance from a trained classification model, ranks the top features,
and visualizes them in a horizontal bar chart. Optionally, the plot can be saved to a PDF file.
}
\examples{
\dontrun{
# Assuming `model` is a trained model object:
get_feature_importance(model)
}
}
