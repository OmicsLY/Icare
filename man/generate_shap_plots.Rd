% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-16-shap.R
\name{generate_shap_plots}
\alias{generate_shap_plots}
\title{Generate SHAP Analysis Plots from a Trained Model}
\usage{
generate_shap_plots(
  train_data,
  best_model,
  group_col = "group",
  palette_name = "AsteroidCity1",
  save_plots = TRUE,
  save_dir = here("ModelData", "best_model_result"),
  plot_width = 5,
  plot_height = 5,
  base_size = 14,
  seed = 123
)
}
\arguments{
\item{train_data}{A data frame containing the training features and labels. The response variable should be specified in \code{group_col}.}

\item{best_model}{Currently unused. Reserved for potential future compatibility.}

\item{group_col}{Character. The name of the column in \code{train_data} representing the target variable. Default is \code{"group"}.}

\item{palette_name}{Character. Name of the color palette used for plots from \code{wesanderson}. Default is \code{"AsteroidCity1"}.}

\item{save_plots}{Logical. If \code{TRUE}, plots will be saved as PDF files in the specified directory. Default is \code{TRUE}.}

\item{save_dir}{Character. Path to the directory where plots will be saved. Default is \code{here("ModelData", "best_model_result")}.}

\item{plot_width}{Numeric. Width of the plot in inches. Default is 5.}

\item{plot_height}{Numeric. Height of the plot in inches. Default is 5.}

\item{base_size}{Numeric. Base font size for plot themes. Default is 14.}

\item{seed}{Integer. Random seed for reproducibility. Default is 123.}
}
\value{
A list containing the generated \code{ggplot} objects:
\describe{
\item{beeswarm_plot}{SHAP beeswarm plot for feature importance.}
\item{force_plot}{SHAP force plot showing individual predictions.}
\item{waterfall_plot}{SHAP waterfall plot visualizing cumulative feature effects.}
}
}
\description{
This function fits a LightGBM classification model on the provided training data,
calculates SHAP (SHapley Additive exPlanations) values using kernel approximation,
and generates three visualizations: beeswarm, force, and waterfall plots.
}
\examples{
\dontrun{
# Example usage with training data
train_data <- your_training_data_frame
shap_result <- generate_shap_plots(train_data, best_model = NULL)

# Access plots
shap_result$beeswarm_plot
shap_result$force_plot
shap_result$waterfall_plot
}
}
