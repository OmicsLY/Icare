% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-11.R
\name{FeatureImportance}
\alias{FeatureImportance}
\title{Generate and Visualize Feature Importance for the Best Model}
\usage{
FeatureImportance(
  object,
  top_n = 15,
  palette_name = "AsteroidCity1",
  save_plots = TRUE,
  save_dir = here("ModelData", "best_model_result"),
  plot_width = 5,
  plot_height = 5,
  base_size = 14
)
}
\arguments{
\item{object}{An object of class \code{Model_data}, or a trained model object directly (e.g., from caret or randomForest).}

\item{top_n}{Integer specifying the number of top features to display. Default is 15.}

\item{palette_name}{Character string specifying the color palette name from \code{wesanderson}. Default is \code{"AsteroidCity1"}.}

\item{save_plots}{Logical indicating whether to save the plot to file. Default is \code{TRUE}.}

\item{save_dir}{Directory path where plots will be saved. Default is \code{here("ModelData", "best_model_result")}.}

\item{plot_width}{Width of the saved plot in inches. Default is 5.}

\item{plot_height}{Height of the saved plot in inches. Default is 5.}

\item{base_size}{Base font size for the plot theme. Default is 14.}
}
\value{
If \code{object} is of class \code{Model_data}, returns the updated object with a feature importance plot stored in the \verb{@best.model.result$top_features_plot} slot.
If a model object is provided directly, returns a \code{ggplot} object of the importance plot.
}
\description{
This function calculates and visualizes the feature importance of the best model
stored within a \code{Model_data} object or from a model object directly. It supports
saving the plot and storing the result in the object for further use.
}
\examples{
\dontrun{
# If `model_data` is a Model_data object
updated_object <- FeatureImportance(model_data)

# If `rf_model` is a trained random forest model
FeatureImportance(rf_model, top_n = 10, save_plots = FALSE)
}
}
