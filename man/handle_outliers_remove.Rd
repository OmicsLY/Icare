% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-2.R
\name{handle_outliers_remove}
\alias{handle_outliers_remove}
\title{Remove Rows with Outliers from the Data Set}
\usage{
handle_outliers_remove(data)
}
\arguments{
\item{data}{A data frame containing outlier flags (columns with names ending in "_outlier").}
}
\value{
A data frame with rows containing outliers removed.
}
\description{
This function removes rows from the provided data frame that are marked as outliers.
Outlier identification is based on columns with names ending in "_outlier".
The rows with any \code{TRUE} values in these columns will be excluded from the output.
}
\examples{
# Example usage
data <- data.frame(gene1 = c(1, 2, 3, 100, 5),
                   gene1_outlier = c(FALSE, FALSE, FALSE, TRUE, FALSE),
                   gene2 = c(2, 4, 6, 100, 8),
                   gene2_outlier = c(FALSE, FALSE, FALSE, TRUE, FALSE))
cleaned_data <- handle_outliers_remove(data)
# cleaned_data will contain the rows without outliers.
}
