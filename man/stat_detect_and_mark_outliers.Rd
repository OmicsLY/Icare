% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-2.R
\name{stat_detect_and_mark_outliers}
\alias{stat_detect_and_mark_outliers}
\title{Detect and Mark Outliers in a Data Set}
\usage{
stat_detect_and_mark_outliers(
  object,
  save_outliers = TRUE,
  nthreads = 1,
  group_col = "group",
  palette_name = "Royal1",
  handle_method = c("replace", "remove", "keep", "capping"),
  lower_quantile = 0.05,
  upper_quantile = 0.95,
  save_plots = TRUE,
  save_dir = here("StatObject", "pre_outlier_info"),
  plot_display_num = 1,
  sub_var = NULL,
  plot_width = 5,
  plot_height = 5,
  base_size = 14
)
}
\arguments{
\item{object}{Input object, which can either be a \code{Stat} object or a data frame.}

\item{save_outliers}{Boolean indicating whether to save outliers information (default is TRUE).}

\item{nthreads}{Number of threads to use for parallel processing (default is 1).}

\item{group_col}{Name of the grouping column for categorical variables (default is "group").}

\item{palette_name}{Color palette name used for visualization (default is "Royal1").}

\item{handle_method}{Method for handling outliers. Options include "replace", "remove", "keep", and "capping".}

\item{lower_quantile}{The lower quantile used to identify outliers (default is 0.05).}

\item{upper_quantile}{The upper quantile used to identify outliers (default is 0.95).}

\item{save_plots}{Boolean indicating whether to save plots of outlier visualization (default is TRUE).}

\item{save_dir}{Directory where the plots will be saved (default is "StatObject/pre_outlier_info").}

\item{plot_display_num}{Number of plots to display in one batch (default is 5).}

\item{sub_var}{A subset of variables to process (default is NULL, meaning all variables).}

\item{plot_width}{Width of the generated plots (default is 5).}

\item{plot_height}{Height of the generated plots (default is 5).}

\item{base_size}{Base font size for plots (default is 14).}
}
\value{
Returns a modified object with marked outliers, either as a \code{Stat} object or as a data frame.
}
\description{
Detect and Mark Outliers in a Data Set
}
\examples{
# Example 1: Using a Stat object
stat_object <- Stat$new(clean.data = your_data_frame, group_col = "group")
result <- stat_detect_and_mark_outliers(stat_object,
                                        save_outliers = TRUE,
                                        nthreads = 2,
                                        group_col = "group",
                                        save_plots = TRUE)
# The function updates the 'Stat' object and returns it with marked outliers.
# Example 2: Using a data frame directly
data_frame <- data.frame(
  gene1 = c(1, 2, 3, 100, 5),
  gene2 = c(2, 4, 6, 100, 8),
  group = c("A", "A", "B", "B", "A")
)
result_df <- stat_detect_and_mark_outliers(data_frame,
                                          save_outliers = TRUE,
                                          plot_display_num = 2,
                                          group_col = "group")
# The function returns a data frame with outliers marked.
}
