% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-10-sel-feature.R
\name{plot_num_auc}
\alias{plot_num_auc}
\title{Plot AUC vs Number of Features}
\usage{
plot_num_auc(
  combined_plot_data,
  best_features_num,
  palette_name = "AsteroidCity1",
  base_size = 14,
  save_plots = TRUE,
  save_dir = here::here("ModelData", "sel_feature"),
  plot_width = 5,
  plot_height = 5
)
}
\arguments{
\item{combined_plot_data}{A data.frame containing plotting data with columns:
\itemize{
\item Method: Feature selection method (IV/MIC/MI)
\item Num_Features: Number of features used
\item AUC: Corresponding AUC value
\item dAUC: AUC change from previous feature count (optional)
}}

\item{best_features_num}{Integer specifying the optimal number of features identified}

\item{palette_name}{Character name of color palette (default: "AsteroidCity1").
Must be a valid palette from \code{wesanderson} package.}

\item{base_size}{Base font size for plot elements (default: 14)}

\item{save_plots}{Logical indicating whether to save plot to file (default: TRUE)}

\item{save_dir}{Directory path to save plot (default: here::here("ModelData", "sel_feature"))}

\item{plot_width}{Plot width in inches (default: 5)}

\item{plot_height}{Plot height in inches (default: 5)}
}
\value{
A ggplot object containing the performance comparison plot. When \code{save_plots=TRUE},
also saves a PDF file to the specified directory.
}
\description{
Creates a visualization of model performance (AUC) against the number of features
used, highlighting the optimal number of features identified during feature selection.
The plot compares multiple feature selection methods (IV, MIC, MI) and can save
the output to PDF.
}
\examples{
\dontrun{
# After running SelFeatureSet
plot_data <- object@feature.selection$plot_auc_features$data
best_n <- object@feature.selection$best_features$best_features_num

plot_num_auc(
  combined_plot_data = plot_data,
  best_features_num = best_n,
  palette_name = "Royal1",
  save_dir = "results/feature_selection"
)
}
}
