% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-14.R
\name{plot_ppv_vs_threshold}
\alias{plot_ppv_vs_threshold}
\title{Plot PPV vs Threshold Curve (Closest to 0.95)}
\usage{
plot_ppv_vs_threshold(
  threshold_metrics,
  title = "PPV vs Threshold (Closest to 0.95)",
  theme = theme_scp(),
  palette_name = "Zissou1",
  base_size = 14,
  save_plots = TRUE,
  save_dir = here("ModelData", "best_model_result"),
  plot_width = 5,
  plot_height = 5
)
}
\arguments{
\item{threshold_metrics}{A data frame containing the threshold values and corresponding
PPV metrics. The data frame must have at least two columns: 'Threshold' and 'PPV'.}

\item{title}{A character string specifying the title of the plot. Default is "PPV vs Threshold (Closest to 0.95)".}

\item{theme}{A ggplot2 theme object for customizing the plot appearance. Default is \code{theme_scp()}.}

\item{palette_name}{A character string specifying the color palette for the plot. Default is "Zissou1".}

\item{base_size}{An integer specifying the base font size for the plot elements. Default is 14.}

\item{save_plots}{A logical value indicating whether to save the plot. Default is TRUE.}

\item{save_dir}{A character string specifying the directory where the plot will be saved.
Default is \code{"ModelData/best_model_result"}.}

\item{plot_width}{An integer specifying the width of the saved plot in inches. Default is 5.}

\item{plot_height}{An integer specifying the height of the saved plot in inches. Default is 5.}
}
\value{
A list containing:
\item{plot}{The ggplot2 object of the PPV vs threshold plot.}
\item{threshold}{The threshold value that gives the PPV closest to 0.95.}
}
\description{
This function generates a plot showing the relationship between the positive predictive
value (PPV) and different threshold values. The plot highlights the threshold that gives
the PPV closest to 0.95 and annotates it on the graph.
}
\examples{
\dontrun{
# Example usage:
threshold_metrics <- data.frame(Threshold = seq(0, 1, by = 0.05),
                                 PPV = runif(21, 0.5, 1))
plot_results <- plot_ppv_vs_threshold(threshold_metrics)
}
}
