% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-10-sel-feature.R
\name{SelFeatureSet}
\alias{SelFeatureSet}
\title{Feature Selection and Performance Evaluation}
\usage{
SelFeatureSet(
  object = NULL,
  max_features = NULL,
  xintercept = NULL,
  AUC_change_threshold = 0.01,
  group_col = "group",
  palette_name = "AsteroidCity1",
  data_type = "clean",
  save_plots = TRUE,
  save_dir = here::here("ModelData", "sel_feature"),
  plot_width = 5,
  plot_height = 5,
  base_size = 14,
  seed = 123,
  csv_filename = "feature_selection_results.csv",
  save_data = TRUE
)
}
\arguments{
\item{object}{A \code{Train_Model} object containing training data or a list with
\code{features} and \code{label} components. If NULL, \code{features} and \code{label} must be provided.}

\item{max_features}{Maximum number of features to consider (default: NULL, uses all features).}

\item{xintercept}{X-axis intercept for plot (default: NULL, uses max_features).}

\item{AUC_change_threshold}{Minimum AUC improvement threshold for feature selection
(default: 0.01).}

\item{group_col}{Name of the response variable column (default: "group").}

\item{palette_name}{Name of color palette for plotting (default: "AsteroidCity1").}

\item{data_type}{Type of data to use: "clean" for raw or "scale" for standardized
(default: "clean").}

\item{save_plots}{Logical indicating whether to save plots (default: TRUE).}

\item{save_dir}{Directory to save results (default: here::here("ModelData", "sel_feature")).}

\item{plot_width}{Plot width in inches (default: 5).}

\item{plot_height}{Plot height in inches (default: 5).}

\item{base_size}{Base font size for plots (default: 14).}

\item{seed}{Random seed for reproducibility (default: 123).}
}
\value{
Depending on input type:
\itemize{
\item For \code{Train_Model} object: Returns updated object with feature selection results stored in:
\itemize{
\item \verb{@feature.selection}: Detailed selection results
\item \verb{@feature.result}: Best feature subset
}
\item For other inputs: Returns list with:
\itemize{
\item \code{plot}: ggplot object of performance comparison
\item \code{feature_data}: Combined performance data for all methods
}
}
}
\description{
Performs feature selection using three different methods (Information Value, Maximal
Information Coefficient, and Mutual Information) and evaluates their performance based
on AUC metrics. Identifies the optimal number of features and generates visualization.
}
\details{
The function performs the following steps:
\enumerate{
\item Validates input data and parameters
\item Runs three feature selection methods (IV, MIC, MI)
\item Calculates AUC performance for each method across feature counts
\item Identifies optimal feature set based on AUC improvement threshold
\item Generates performance comparison plot
\item Updates input object or returns results
}
}
\section{Output Files}{

\itemize{
\item \code{feature_selection_results.csv}: CSV file with performance metrics
\item \code{Combined_AUC_vs_Features.pdf}: Plot of AUC vs feature count (if save_plots=TRUE)
}
}

\examples{
\dontrun{
# With Train_Model object
# Custom parameters
object_model<-SelFeatureSet(
  object = object_model,
  max_features = 50,
  AUC_change_threshold = 0.005,
  palette_name = "Royal1"
)
}
}
