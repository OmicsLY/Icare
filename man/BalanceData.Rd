% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-8-balance.R
\name{BalanceData}
\alias{BalanceData}
\title{Balance Training Dataset Using Various Sampling Methods}
\usage{
BalanceData(
  object,
  group_col = "group",
  method = "both",
  N = NULL,
  palette_name = "Royal1",
  seed = 123,
  imbalance_threshold = 0.15,
  sample_size_threshold = 1500,
  force_balance = FALSE,
  save_plots = TRUE,
  save_dir = here::here("ModelData", "Data"),
  plot_width = 5,
  plot_height = 5,
  base_size = 14,
  K = 5,
  dup_size = 0,
  save_data = TRUE,
  train_filename = "train_data.csv"
)
}
\arguments{
\item{object}{Either a Train_Model object or a list containing training data and optionally
testing/validation data}

\item{group_col}{Character string specifying the name of the grouping variable (default: "group")}

\item{method}{Balancing method to use: "over" (oversampling), "under" (undersampling),
"both" (combined sampling), or "smote" (SMOTE algorithm) (default: "both")}

\item{N}{Desired sample size after balancing (NULL for automatic calculation based on method)}

\item{palette_name}{Color palette name for visualization plots (default: "Royal1")}

\item{seed}{Random seed for reproducibility (default: 123)}

\item{imbalance_threshold}{Minimum class ratio threshold to trigger balancing
(default: 0.15, range 0-1)}

\item{sample_size_threshold}{Maximum sample size threshold to apply balancing
(default: 1500 observations)}

\item{force_balance}{Logical, whether to force balancing regardless of thresholds
(default: FALSE)}

\item{save_plots}{Logical, whether to save balance visualization plots (default: TRUE)}

\item{save_dir}{Directory path to save plots and CSV files (default: here::here("Data","ModelData"))}

\item{plot_width}{Plot width in inches (default: 5)}

\item{plot_height}{Plot height in inches (default: 5)}

\item{base_size}{Base font size for plots (default: 14)}

\item{K}{Number of nearest neighbors to consider for SMOTE (default: 5)}

\item{dup_size}{Oversampling rate for SMOTE (0 = balanced, 1 = double minority class, etc.)}

\item{save_data}{Logical, whether to save balanced training data as CSV file (default: TRUE)}

\item{train_filename}{Name for the output CSV file containing balanced training data
(default: "train_data.csv")}
}
\value{
Depending on input type:
\itemize{
\item For Train_Model objects: Returns updated object with balanced training data in split.data slot
and balancing information in process.info slot
\item For lists: Returns a list containing balanced training data and unchanged other datasets,
along with balancing parameters
}
}
\description{
This function performs class balancing on the training dataset only (while keeping test/validation
sets unchanged) using various sampling techniques including oversampling, undersampling,
combined sampling, or SMOTE (Synthetic Minority Oversampling Technique). It handles both
Train_Model objects and raw data list inputs, with optional CSV export of the balanced training data.
}
\examples{
\dontrun{
# Example with Train_Model object
object_model <- BalanceData(
  object = object_model,
  method = "smote",
  imbalance_threshold = 0.2
)

}
}
