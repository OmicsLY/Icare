% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-4-val-add.R
\name{Extract_external_validation}
\alias{Extract_external_validation}
\title{Extract and Prepare External Validation Data for Model Evaluation}
\usage{
Extract_external_validation(
  data = NULL,
  object_stats = NULL,
  object_model = NULL,
  group_col = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame containing the external validation data. Either this or \code{object_stats} must be provided.}

\item{object_stats}{A Stat object from which clean data can be extracted. Either this or \code{data} must be provided.}

\item{object_model}{A Train_Model object that will be updated with the external validation data.}

\item{group_col}{The name of the column containing group/class information (optional).}

\item{...}{Additional arguments passed to diagnostic and conversion functions.}
}
\value{
The updated \code{object_model} with the external validation data added to its \code{split.data} slot.
}
\description{
This function processes external validation data (either provided directly or extracted from a Stat object)
and prepares it for use with a trained model object. It performs data cleaning, type conversion,
one-hot encoding, and ensures compatibility with the model's requirements.
}
\examples{
\dontrun{
# Example with direct data input
model <- Extract_external_validation(
  data = validation_data,
  object_model = trained_model,
  group_col = "diagnosis"
)

# Example with Stat object input
model <- Extract_external_validation(
  object_stats = stat_object,
  object_model = trained_model
)
}

}
