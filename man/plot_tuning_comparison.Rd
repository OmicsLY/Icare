% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-13-Tune.R
\name{plot_tuning_comparison}
\alias{plot_tuning_comparison}
\title{Plot ROC Curve Comparison Between Original and Tuned Models}
\usage{
plot_tuning_comparison(
  original_model,
  tuned_model,
  test_data,
  group_col = "group",
  palette_name = "AsteroidCity1",
  base_size = 14,
  save_plots = TRUE,
  save_dir = here("ModelData", "tuning_results"),
  plot_width = 5,
  plot_height = 5,
  alpha = 1,
  plot_title = "ROC Curve Comparison: Before vs After Tuning"
)
}
\arguments{
\item{original_model}{The original model object (must have predict method for type = "prob")}

\item{tuned_model}{The tuned model object (must have predict method for type = "prob")}

\item{test_data}{Data frame containing test data with response variable}

\item{group_col}{Character string specifying the column name of the binary response variable (default = "group")}

\item{palette_name}{Character string specifying the Wes Anderson palette name (default = "AsteroidCity1")}

\item{base_size}{Base font size for the plot (default = 14)}

\item{save_plots}{Logical indicating whether to save plots (default = TRUE)}

\item{save_dir}{Directory path where plots should be saved (default = here("ModelData", "tuning_results"))}

\item{plot_width}{Width of the plot in inches (default = 5)}

\item{plot_height}{Height of the plot in inches (default = 5)}

\item{alpha}{Transparency level for plot lines (default = 1)}

\item{plot_title}{Title for the plot (default = "ROC Curve Comparison: Before vs After Tuning")}
}
\value{
Invisibly returns a list containing:
\itemize{
\item roc_objects - List of ROC curve objects for both models
\item auc_values - Named vector of AUC values
}
}
\description{
This function generates ROC curves comparing the performance of an original model
versus a tuned model on test data, including AUC values and confidence intervals.
The plot can be customized and optionally saved to disk.
}
\examples{
\dontrun{
# Assuming you have original_model, tuned_model, and test_data prepared
results <- plot_tuning_comparison(
  original_model = original_rf,
  tuned_model = tuned_rf,
  test_data = test_data
)
}
}
