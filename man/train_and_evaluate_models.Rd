% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-8.R
\name{train_and_evaluate_models}
\alias{train_and_evaluate_models}
\title{Train and Evaluate Multiple Models Using Caret}
\usage{
train_and_evaluate_models(
  data,
  methods,
  control,
  tune_grids,
  classProbs = TRUE,
  verboseIter = FALSE,
  allowParallel = TRUE,
  group_col = "group"
)
}
\arguments{
\item{data}{A data frame containing the training data. The target variable should
be specified in the \code{group_col} parameter and must be a factor.}

\item{methods}{A vector of model names (strings) to train using the \code{caret} package.
These should match the model names in \code{caret::modelLookup()}.}

\item{control}{A list containing control parameters for model training. Should
include at least \code{method}, \code{number}, and \code{repeats} for the \code{trainControl()} function.}

\item{tune_grids}{A list where each element corresponds to a tuning grid for
the respective model in \code{methods}. Each grid is passed to the \code{tuneGrid} parameter
in the \code{train()} function.}

\item{classProbs}{A logical value indicating whether class probabilities should
be computed. Default is \code{TRUE}.}

\item{verboseIter}{A logical value indicating whether iteration progress should
be displayed during model training. Default is \code{FALSE}.}

\item{allowParallel}{A logical value indicating whether parallel processing
should be used. Default is \code{TRUE}.}

\item{group_col}{The name of the target variable (the outcome or response variable)
in the \code{data} set. Default is \code{"group"}.}
}
\value{
A list of trained models, where each element is a model object corresponding
to a method from \code{methods}. If training fails for a method, a warning is issued
and that model is not included in the results.
}
\description{
This function trains and evaluates multiple machine learning models using the
\code{caret} package. It accepts multiple model types, tuning grids, and training control
parameters, and returns a list of trained models.
}
\examples{
# Example usage:
data(iris)
methods <- c("rf", "rpart")
control <- list(method = "cv", number = 10, repeats = 3)
tune_grids <- list(rf = expand.grid(mtry = c(1, 2, 3)), rpart = expand.grid(cp = c(0.01, 0.1)))
results <- train_and_evaluate_models(iris, methods, control, tune_grids)
}
